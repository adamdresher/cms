<!doctype html>
<html>
  <body>
    <ul>
      <% @files.each do |file| %>
        <li>
          <a href="<%= "#{@root_dir}/public/data/#{file}" %>" ><%= file %></a>
        </li>
      <% end %>
    </ul>
  </body>
</html>
